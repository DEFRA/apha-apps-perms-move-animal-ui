name: Browserstack Tests

on:
  workflow_dispatch:
  workflow_run:
    workflows: [ 'Publish' ]
    types:
      - completed
jobs:
  test:
    uses: defra/apha-apps-perms-move-animal-uat/.github/workflows/run-tests-browserstack.yaml@main
    secrets:
      BROWSERSTACK_USER: ${{ secrets.BROWSERSTACK_USER }}
      BROWSERSTACK_KEY: ${{ secrets.BROWSERSTACK_KEY }}
