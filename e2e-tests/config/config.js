export default {
  logLevel: 'silent',
  maxInstance: 5,
  bail: 0,
  specFileRetries: 0,
  specFileRetriesDeferred: true,
  waitForTimeout: 10000,
  waitforInterval: 200,
  connectionRetryTimeout: 120000,
  connectionRetryCount: 3
}
