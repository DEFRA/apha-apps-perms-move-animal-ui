import { describePageSnapshot } from '~/src/server/common/test-helpers/snapshot-page.js'
import { Answer, {{pageName}}Page } from './index.js'
import { TodoAnswer } from '~/src/server/common/model/answer/todo/todo.js'

const sectionKey = '{{sectionKey}}'
const questionKey = '{{questionKey}}'
const pageUrl = '{{urlPath}}'
const question = '{{question}}'

const page = {{pageName}}Page

const payload = {
  [questionKey]: undefined // TODO - define valid payload
}

describe('Answer', () => {
  it('should be of the right type', () => {
    expect(new Answer(payload)).toBeInstanceOf(TodoAnswer)
  })

  it('should have the right payload key', () => {
    expect(Answer.config.payloadKey).toBe(questionKey)
  })

  //... TODO - insert other option tests
})

describe('EnterWhatIsMovingPage', () => {
  it('should have the correct urlPath', () => {
    expect(page.urlPath).toBe(pageUrl)
  })

  it('should have the correct question', () => {
      expect(page.question).toBe(question)
  })

  it('should have the correct sectionKey', () => {
    expect(page.sectionKey).toBe(sectionKey)
  })

  it('should have the correct questionKey', () => {
    expect(page.questionKey).toBe(questionKey)
  })

  it('should have the correct Answer model', () => {
    expect(page.Answer).toBe(Answer)
  })

  describe('nextPage', () => {
    it('should return TODO for TODO', () => {
      // ... TODO - insert next page
    })
  })

  describePageSnapshot({
    describes: 'content',
    it: 'should render expected response and content',
    pageUrl
  })
})
